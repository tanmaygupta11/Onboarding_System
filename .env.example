# Used by docker compose when building the frontend image.
# Leave empty to use same-origin requests like /api/me through frontend Nginx.
# For split production domains, set the backend origin only; do not append /api.
VITE_API_BASE_URL=https://awign-onboarding-system-api.awignhub.in
